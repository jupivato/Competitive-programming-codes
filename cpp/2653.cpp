#include <bits/stdc++.h>

using namespace std;

int main()
{ 
    string joia; 
    set <string > colecao; 
    while (cin >> joia) colecao.insert(joia);
    cout << colecao.size() << endl;

return 0;
}